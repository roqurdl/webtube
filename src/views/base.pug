doctype html
html(lang="ko")
    head
        title #{pageTitle} | Webtube
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
    body
        header
            h1=pageTitle
            if pageTitle !== "Home" 
                nav 
                    a(href="/") Home
            block link
        main
            block content
    include partials/footer